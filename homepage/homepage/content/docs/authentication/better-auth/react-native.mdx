# Better Auth

We do not currently support using Better Auth with React Native.
We do have support for [React](/docs/react/authentication/better-auth), [Expo](/docs/react-native-expo/authentication/better-auth), and [vanilla JavaScript](/docs/vanilla/authentication/better-auth).
